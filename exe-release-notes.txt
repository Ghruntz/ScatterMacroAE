Tower now uses a dedicated saved Select Stage position. Use SET TOWER SELECT STAGE in Tower mode to capture it. Removed development-only OCR overlays and added Explorer icon-cache refresh after EXE updates.