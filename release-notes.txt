v1.0.170
- Expedition difficulty selection now reads the purple skull row directly and proceeds in 100ms click cycles.
- Difficulty selection begins 60ms after the map is clicked.
- OCR remains only as a final fallback for non-standard UI layouts.
